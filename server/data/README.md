# Server Data

The server currently reads plant metadata from `../src/data/plants.json` so the demo keeps a single source of truth.

Move shared data here only if the backend becomes the authoritative data owner.
