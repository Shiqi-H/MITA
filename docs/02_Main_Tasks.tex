\documentclass[12pt,a4paper]{article}

\usepackage[margin=2.5cm]{geometry}
\usepackage{graphicx}
\usepackage{setspace}
\usepackage{times}
\usepackage{array}
\usepackage{enumitem}

\newcommand{\tasksub}[1]{\par\smallskip\noindent\textbf{#1}\quad\ignorespaces}

\begin{document}

\begin{titlepage}
    \thispagestyle{empty}

    % Header
    \noindent
    \begin{minipage}{0.5\textwidth}
        \includegraphics[width=0.8\textwidth]{Politecnico_di_Milano.png}
    \end{minipage}
    \hfill
    \begin{minipage}{0.5\textwidth}
        \raggedleft
        \vspace{0.3cm}
        {\large\bfseries
        MULTIMODAL INTERACTIVE TECHNOLOGIES AND APPLICATIONS}
    \end{minipage}

    \vspace{4.0cm}

    % Title
    \begin{center}
        {\Huge\bfseries Main Tasks for Heuristic Evaluation}\\[0.8cm]
        {\Large Botanical Garden}\\
    \end{center}

    \vfill

    % Group information
    \begin{center}
        \renewcommand{\arraystretch}{2.05}
        \begin{tabular}{>{\raggedleft}p{5cm} p{1.5cm} p{5cm}}
            {\Large Group Name} & & {\Large Gardeners} \\
            {\Large Kaifei Xu } & & {\Large 11115439} \\
            {\Large Keyan Cheng} & & {\Large 11154362} \\
            {\Large Shiqi Huang} & & {\Large 11111452} \\
            {\Large Yufei Liu} & & {\Large 11118413} \\
        \end{tabular}
    \end{center}

    \vspace{3.2cm}

    % Date
    \begin{center}
        May 25, 2026
    \end{center}

\end{titlepage}

\newpage

This document describes the three interaction tasks on which the evaluating
team is expected to run their heuristic evaluation. Each task gives the
user goal, the modalities involved, the expected interaction flow and the
success criteria. All tasks take place inside the immersive A-Frame
botanical garden after the loading screen dismisses and the first
panoramic scene is visible.

\section{Task 1 --- Disambiguation of Overlapping Plants}
\tasksub{Goal.} Ask a question about \textbf{one specific plant} in a
region where several specimens visually overlap (e.g.\ the cluster of
Lavandula, Nephrolepis, and Santolina near the right path in scene~1).

\tasksub{Modalities.} \emph{Input:} pointer raycasting + speech (STT) +
click fallback on the A\,/\,B overlay. \emph{Output:} 3D highlight on every
candidate, modal overlay with labelled options, and a TTS clarification
question.

\tasksub{Flow.}
\begin{enumerate}
    \item User points at the overlapping plants and clicks (or speaks while
    gazing).
    \item The frontend collects all raycaster hits and stores the candidates
    in client-side state. If more than one candidate is detected, the
    ambiguity flow starts immediately on the client.
    \item The frontend sorts candidates by depth (closest first), labels
    them A, B\ldots, and requests a spoken clarification prompt from the
    backend (\emph{``I can see two plants --- do you mean A, the one in
    front, or B, behind it?''}).
    \item The frontend highlights every candidate, opens the A\,/\,B
    overlay and plays the TTS prompt.
    \item User resolves by voice (\emph{``A''}, \emph{``the one behind''},
    \emph{``lavender''}) or by clicking; the frontend sends the utterance
    and candidate context to the backend for resolution.
    \item The backend resolves the utterance against the candidates and
    returns a structured response; the information card is rendered and
    the overlay is dismissed.
\end{enumerate}

\tasksub{Success criteria.} Highlight, overlay and TTS appear within
$\sim$1\,s of the click; voice resolution succeeds for natural phrasings
(\emph{``A''}, \emph{``the front one''}, plant name); if unresolved, a
retry prompt with softer wording is issued and after \textbf{two} failed
retries the user is guided to the click buttons; no information card is
rendered until the ambiguity is resolved.

\tasksub{Heuristics.} Match between system and the real world; visibility
of system status; user control and freedom (overlay always dismissible);
error prevention and recovery (retry + click fallback).

\section{Task 2 --- Comparing the Current Plant with a Visited One}
\tasksub{Goal.} After inspecting at least one plant, \textbf{compare} a
newly selected plant with one seen earlier (\emph{``How does this compare
to the lavender I just looked at?''}).

\tasksub{Modalities.} \emph{Input:} pointer raycasting to lock onto the new
plant + speech (free-form comparison query). \emph{Output:} a dedicated
comparison card rendered side-by-side, plus a TTS summary.

\tasksub{Flow.}
\begin{enumerate}
    \item User has previously selected at least one plant; its identity is
    stored in the client-side visited plant list.
    \item User clicks the second plant and asks a comparison-flavoured
    question (\emph{``compare it with the lavender''}, \emph{``which one is
    more drought-tolerant?''}).
    \item The frontend parses the intent and routes to the comparison
    handler, passing the utterance, selected plant and visited history as
    request context.
    \item The backend identifies the comparison intent and resolves the
    target plant by name, alias or recency.
    \item The backend fetches both plants, optionally filters by the
    detected attribute of interest (e.g.\ medicinal use, drought
    tolerance), and returns a comparison response with a spoken summary.
    \item The frontend renders a side-by-side comparison card and plays
    the TTS narration.
\end{enumerate}

\tasksub{Success criteria.} Implicit references (\emph{``the previous
one''}, \emph{``lavender''}) are resolved via the visited list; the card
visually aligns matching attributes (height, toxicity, drought tolerance,
family\ldots); comparison only triggers when the user is \textbf{not
locked} to a single plant via the contextual follow-up path (preventing
accidental comparisons in a focused Q\&A); a graceful error is returned
when no valid second plant can be identified.

\tasksub{Heuristics.} Recognition rather than recall (visited plants are
remembered for the user); consistency and standards (same card layout as
Task~3); aesthetic and minimalist design (only the requested attribute is
compared, not the full record).

\section{Task 3 --- Dynamic Information Card Driven by Spoken Intent}
\tasksub{Goal.} Obtain \textbf{targeted information} about a single plant
--- botanical description, medicinal use, toxicity, drought tolerance,
feng-shui symbolism --- without navigating menus.

\tasksub{Modalities.} \emph{Input:} pointer raycasting to select the plant
+ speech (STT); follow-up turns include the selected plant as context
so the system stays focused on the displayed plant. \emph{Output:} a
context-sensitive card whose layout depends on the detected topic of
interest (medicinal, botanical, or general), a 3D highlight, and a TTS
answer.

\tasksub{Flow.}
\begin{enumerate}
    \item User selects a plant; the frontend records the visit in
    client-side state and the system parses the user's intent and topic
    of interest.
    \item The detected topic is mapped to a card type: medicinal, botanical,
    or general information.
    \item If the requested topic is unavailable for that plant (e.g.\ feng
    shui for a plant without that data), the backend returns a fallback
    response listing the available topics, plus a polite spoken
    explanation.
    \item The frontend renders an information card for normal answers, or a
    fallback card with alternative topic buttons (each of which re-issues
    a request for the same plant).
    \item Subsequent utterances are treated as \textbf{contextual
    follow-ups}; dialogue history is passed as context for coherent
    multi-turn answers.
\end{enumerate}

\tasksub{Success criteria.} Card type changes with spoken intent (e.g.\
\emph{``is it safe for my dog?''} surfaces toxicity content, not the
default description); the fallback path never dead-ends --- at least one
alternative topic is always offered; multi-turn follow-ups remain locked
to the selected plant, and switching plants clears the dialogue panel;
TTS, card and 3D highlight stay synchronised across turns.

\tasksub{Heuristics.} Flexibility and efficiency of use; help users
recognise and recover from errors (fallback topics); consistency between
voice and visual answers; minimalist design of each card.

\section*{Out-of-Scope}
\textbf{Not} part of this heuristic evaluation: \emph{scene navigation}
between the five panoramic environments via arrow portals (evaluated only
as a pre-condition for reaching the plants); \emph{STT/TTS audio quality}
itself (inherits the browser-native Web Speech API); \emph{LLM answer
factuality} (bounded by the curated plant database, not a target of the
UX evaluation).

\end{document}
