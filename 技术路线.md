# 全景环境 + 局部 3D 对象

## 1 全景环境
### 1.1 目标
构建虚拟植物园环境，实现“谷歌街景”效果，用户可以在其中“走动”，并自由转动视角。

### 1.2 实现
- **素材准备**：拍摄 360° 完整球形全景照片。
- **3D 引擎渲染**：基于 WebGL 的 JavaScript 3D 库 Three.js，在浏览器前端构建虚拟球体（SphereGeometry）或圆柱体。将全景照片作为内部纹理进行贴图，并将虚拟摄像机放置在几何体中心点。

### 1.3 问题
- 横向全景照片是否可行？是否需要全景相机来拍摄？

## 2 局部 3D 对象
### 2.1 目标
在全景环境中嵌入特定的局部 3D 植物模型以及兴趣点，为系统提供精确的“视觉指代物”。 支持区域选择与对话多模态交互，使用户能够通过“点击 + 语音”的并发输入与植物进行上下文对话。

### 2.2 实现
- **3D 资产采集与创建**：对于需要重点交互的植物（如盆栽），进行摄影测量，生成 3D 网格模型（Mesh）。并在 Three.js 中定义三维坐标，添加悬浮的虚拟兴趣点。
- **场景集成与射线检测**： 将生成的局部 3D 模型和兴趣点放置在 Three.js 渲染的全景球体内部的对应位置。利用 Three.js 的 Raycaster（射线检测）技术，监听用户在 3D 漫游时的鼠标点击或触控（Touch）事件，从而捕获用户所指的目标对象。
- **多模态深度融合**：系统在前端通过事件循环（Event Loop）与 Promises 并发处理用户的多模态输入。当用户点击某个 3D 兴趣点（提供了“是什么/What”的视觉证据）并同时语音提问（如“这种植物有毒吗？”，提供了“意图/Intent”和约束）时，系统将这两者的数据统一发送给后端大语言模型进行融合解析，生成针对该特定植物和特定问题的智能回答。点击兴趣点 → 指定兴趣区域 → 语言/文本输入 → 系统解释 → 输出结果

### 2.3 问题
- 如何制造“歧义”？在某些视角下，多个植物的 3D 模型可能会出现重叠。如果用户的点击不够精确，射线检测会返回多个置信度接近的候选目标。
- 修复机制怎么做？使用 AR 高亮框同时标记两个候选植物，并语音反问，要求用户进一步解释。
- 鲁棒性？若系统无法理解语音输入（嘈杂环境），该切换什么后备机制？文本输入还是鼠标点击？
